import React from 'react';
import LoginFailMailPresenter from './LoginFailMailPresenter';

const LoginFailMailContainer = () => {
  return <LoginFailMailPresenter />;
};

export default LoginFailMailContainer;
